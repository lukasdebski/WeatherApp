import React from 'react';

import "./Header.scss"

const Header = () => {
    return (
        <header className="App-header">
            <h1>Lukasz, brawo! Dziala!!! :)</h1>
        </header>
    );
};
export default Header;